def oxford_comma(array)
  if array.length == 1
    return "#{array[0]}"
  elsif array.length == 2
    return "#{array[0]} and #{array[2]}" # add an "and" here
  else
    #do a think for adding commas here
  end
end